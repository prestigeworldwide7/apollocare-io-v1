[package]
name = "apollo_core"
version = "0.1.0"
edition = "2021"

[lib]
name = "apollo_core"
crate-type = ["cdylib", "lib"]

[dependencies]
anchor-lang = "0.29.0"
anchor-spl = "0.29.0"
bytemuck = "1.14"
num-derive = "0.3"
num-traits = "0.2"

[features]
no-entrypoint = []
